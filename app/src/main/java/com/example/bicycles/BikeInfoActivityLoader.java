package com.example.bicycles;

import android.content.Intent;

public interface BikeInfoActivityLoader {
    // Role is to start a new BikeInfoActivity

    public void loadBikeInfoActivity(int position);

}
